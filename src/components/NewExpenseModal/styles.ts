import styled from 'styled-components'

export const FormContainer = styled.div`
  h2 {
    color: var(--white);
    font-size: 2.5rem;
    margin-bottom: 2rem;
    text-align: center;
    @media(max-width: 720px) {
      font-size: 2rem;
      margin-bottom: 1rem;
    }
  }
`
